import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vhb.client.app',
  appName: 'vhb',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
